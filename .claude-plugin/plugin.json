{
  "name": "lathe",
  "description": "Token-efficient MCP tools for Claude Code: fused search, batched fuzzy edit, atomic glob-regex edit, and AST-aware read. Four tools that collapse 3-5 vanilla tool calls down to 1, surfaced eagerly so the model can use them from turn 1 without a ToolSearch promotion round-trip.",
  "version": "0.3.1",
  "keywords": ["search", "edit", "read", "mcp", "token-efficient", "ripgrep", "ast", "tree-sitter"],
  "mcpServers": {
    "tool": {
      "command": "beaglelathe",
      "args": ["serve"],
      "alwaysLoad": true
    }
  }
}
